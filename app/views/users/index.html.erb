<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users"> 
	<% @users.each do |foopar| %>
	 <%= render 'users', user: foopar %>
	<% end %>
</ul>

<%= will_paginate %>